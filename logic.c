#include "defs.h"

void setUpGame() {
    char grid[GRID_LEN][GRID_LEN];
    int mineLocations[NUM_MINES][2];

    for (int i = 0; i > NUM_MINES; i++) {
        //generate position of mine
        //check if it is not a duplicate
    }
}
